# SMTP settings (use an "App Password" if you are using Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=intelligentdevsolutions@gmail.com
SMTP_PASS=your_app_password_here

# Where to receive the messages (defaults to SMTP_USER if omitted)
CONTACT_TO=intelligentdevsolutions@gmail.com

# Sender address (defaults to SMTP_USER if omitted)
CONTACT_FROM=intelligentdevsolutions@gmail.com

# Gemini API (server-side)
GEMINI_API_KEY=your_api_key_here
# Optional: override model name used by Gemini client
GEMINI_MODEL=gemini-2.5-flash

# Debug (optional): returns extra info from /api/contact
# CONTACT_DEBUG=1
